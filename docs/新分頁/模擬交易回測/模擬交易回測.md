# 模擬交易回測頁面架構設計 (詳細完整版)

頁面整體分為 **左側（回測參數設定面板）** 與 **右側（回測報告與分析面板）** 兩大區塊。左側面板負責定義後端模擬迴圈的所有規則，右側面板則負責接收後端運算結果並進行視覺化渲染，風格排版類似股票篩選頁面的左右側架構。

## ▍左側面板：回測參數設定 (Settings Panel)

此區塊為模擬交易規則的控制中心，所有條件皆在後端進行模擬，獨立運作。

### 1. 基本回測參數 (Basic Parameters)
* **回測期間 (Backtest Period)：**
  * 快捷按鈕列：`1D` / `1W` / `1M` / `3M` / `6M` / `1Y` / `自訂`（風格與股票篩選頁面的「分析時間範圍」一致）。
  * 選擇「自訂」時，展開開始日期 (`start_date`) 與結束日期 (`end_date`) 選擇器。
* **執行頻率 (Execution Frequency)：** * 下拉選單：1分 / 3分 / 5分 / 15分 / 30分 / 1H / 4H / 日 (1D) / 週 (1W) / 月 (1M) / 季。
  * *說明：決定後端回測引擎每次推進的時間單位。*
* **交易方向 (Trade Direction)：** * 單選按鈕：作多 (Long) / 作空 (Short)。
* **執行商品範圍 (Execution Instrument Range)：**
  * 選項卡或下拉選單，決定回測的母體池（僅限美股）：
    1. **股票篩選策略：** 匯入「股票篩選」分頁中已儲存的「我的策略」條件，作為動態股票池（每個時間節點只交易符合該策略的股票）。
    2. **自訂股票清單：** 手動輸入或匯入特定股票代碼（例如：AAPL, NVDA, MSFT）。
    3. **全市場：** 針對美股全市場進行掃描。

### 2. 進場設定 (Entry Settings)
* **進場觸發條件 (Entry Triggers)：**
  * UI 支援**摘要模式 / 編輯模式**雙模式切換（與現有股票篩選器的指標/型態設定一致）：
    * **摘要模式 (Summary Mode)**：顯示簡明條件字串（例如：`MA-日K: MA20 > MA60`），右側附帶「✏ 編輯」與「🗑 刪除」Icon。
    * **編輯模式 (Edit Mode)**：點擊「✏ 編輯」或「+ 新增進場條件」時展開卡片，提供指標 / 型態選擇、週期、比較運算子及數值等下拉選單。
* **進場限制 (Entry Restrictions)：**
  * **開盤跳空設定**：組合式選項 — `開盤跳空` `[向上 / 向下 ▼]` `達` `[輸入框]` `%` 即 `[交易 / 不交易 ▼]`
  * **特定時間區段**：組合式選項 — `開盤後` `[輸入框]` `[分鐘 / 小時 ▼]` `[交易 / 不交易 ▼]`
  * *說明：兩組限制條件為「AND」關係，皆啟用時須同時滿足。*
* **進場價格機制 (Entry Price execution)：**
  * 單選按鈕：`次期開盤價 (Next Open)` / `當期收盤價 (Current Close)` / `條件觸發價 (Trigger Price)`
  * *「條件觸發價」說明：適用於僅有日線資料的環境下模擬「盤中突破」。若當日高 / 低點穿越觸發條件的目標價位，則直接以該突破價格作為進場成本（無需等待下根 K 線開盤）。*
* **最大同時持有檔數 (Max Open Positions)：** * 數字輸入框（留空或輸入 0 代表無限制）。

### 3. 出場設定 (Exit Settings)
* **條件出場 (Condition-based Exit)：**
  * UI 支援**摘要模式 / 編輯模式**雙模式切換（與進場觸發條件一致）。
  * 允許設定技術指標或型態作為出場觸發（例如：跌破 20MA、RSI 死亡交叉）。
* **固定停利 / 停損 (Stop Profit / Stop Loss)：**
  * 停利 (Take Profit)：核取方塊 `[ ]` 啟用，輸入 `%` 數。
  * 停損 (Stop Loss)：核取方塊 `[ ]` 啟用，輸入 `%` 數。
* **時間停損 (Time Stop)：**
  * 最大持有時間：核取方塊 `[ ]` 啟用，輸入 `N` 期（依據上方設定的「執行頻率」單位計算）。
* **出場價格機制 (Exit Price execution)：**
  * 單選按鈕：`次期開盤價 (Next Open)` / `當期收盤價 (Current Close)` / `條件觸發價 (Trigger Price)`
  * *「條件觸發價」說明：同進場機制，適用日線資料下模擬當日觸及出場條件價位時直接出場（例如跌破特定支撐線即以穿破價出場）。*

### 4. 資金與交易設定 (Capital & Trading Settings)
* **初始資金 (Initial Capital)：** * 數字輸入框（例如：$1,000,000）。
* **資金與風險管理模組 (Risk Management Module) [Optional]：**
  * 核取方塊 `[ ]` 啟用 / 停用。
  * *說明：啟用後，後端將自動套用與前端「資金與風險管理」分頁完全相同的數學邏輯。系統會依據設定的「單筆停損容忍度 (%)」自動計算每次進場的建議股數，並監控帳戶總風險。若單筆進場會導致帳戶總風險超標，則自動濾除該筆交易。*
* **交易費用 (Trading Costs)：** * 數字輸入框與單位下拉選單：輸入 `%` 數或單筆固定金額（貼合美股券商常見的計費特性）。

---

## ▍核心概念說明：篩選頻率 vs 執行頻率 vs 橋接邏輯

下表說明三個相關但獨立的概念，避免使用者在匯入篩選策略時產生混淆：

| 層級 | 名稱 | 定義位置 | 說明 |
|------|------|----------|------|
| 1 | **篩選頻率 (Screening Frequency)** | 股票篩選頁面（儲存在「我的策略」中） | 定義「這個選股策略多久執行一次市場掃描」（例如：每日、每週）。這是策略本身的一部分，與條件內使用的 K 棒週期無關（例如：可以每週掃描，但條件看日 K 線）。 |
| 2 | **執行頻率 (Execution Frequency)** | 模擬回測頁面 | 定義「回測引擎每次前進的時間單位」，決定系統多久去檢查一次進出場條件是否成立。這是回測的屬性，與選股策略無關。 |
| 3 | **橋接邏輯 (Bridge Logic)** | 匯入策略時的處理 | 當回測頁面匯入某個篩選策略時，該策略的「篩選頻率」預填為目前設定的執行頻率，但使用者可以 override。股票篩選頁面本身是監控當前時間點的工具，策略應為自洽的完整描述，因此篩選頻率應保留在策略中，不應從篩選頁面移除。 |

---

## ▍右側面板：回測報告與分析 (Backtest Report Panel)

點擊「開始回測」後，右側面板將渲染完整報告。建議採用 **頁籤 (Tabs)** 切換或 **區塊向下滾動** 的方式排版。

### 1. 回測基本資訊表頭 (Report Header)
設計為報告最上方的資訊卡，便於快速確認條件或截圖：
* **回測資料範圍：** `YYYY/MM/DD` - `YYYY/MM/DD` (含執行頻率)。
* **回測執行時間：** 報告產生的時間戳記與後端運算耗時。
* **回測執行商品：** 顯示範圍名稱（例：美股全部 / 自訂清單：科技七巨頭），並註記回測成功與失敗的檔數。
* **交易設定摘要：** 顯示交易方向、交易費用、最大持有檔數限制、進/出場價格基準。

### 2. 整體統計表 (Overall Statistics)
核心績效數據總覽，建議包含正負值顏色標示（綠漲紅跌）：
* **交易頻次：** 總交易次數 | 獲利次數 | 虧損次數
* **核心指標：** 勝率 (Win Rate %) | 盈虧比 (Profit/Loss Ratio)
* **報酬與損益分析：** * 總報酬率 (Total Return %)
  * 複合年化成長率 (CAGR)
  * 平均報酬率-單筆 (Avg. Return per Trade %)
  * 平均獲利% (Avg. Win %) | 平均損失% (Avg. Loss %)
  * 平均獲利金額 (Avg. Win $) | 平均損失金額 (Avg. Loss $)
  * 最大獲利-單筆 (Max Profit per Trade %) | 最大虧損-單筆 (Max Loss per Trade %)
* **風險與極端值：** * 最大回撤 (Max Drawdown)
  * 最大連續獲利次數 | 最大連續虧損次數
  * 最大區間獲利率 (Max Interval Profit %) | 最大區間虧損率 (Max Interval Loss %)

### 3. 淨值曲線圖 (Equity Curve)
* **圖表類型：** 互動式折線圖或面積圖。
* **X 軸：** 時間軸（依照回測期間）。
* **Y 軸：** 總報酬率 (%) 或 帳戶總淨值金額。
* **互動功能：** 滑鼠懸停可顯示特定時間點的淨值，圖表中應包含 0% 水平基準線。

### 4. 商品統計表 (Symbol Statistics)
以「單檔股票」為維度的數據表格 (Data Grid)，可點擊欄位標題進行排序：
* 欄位：`股票代碼` | `交易次數` | `勝率` | `總貢獻損益金額` | `總貢獻損益 %`

### 5. 交易明細清單 (Trade Details List)
詳細紀錄每一筆交易的資料表，支援篩選與排序：
* 欄位：
  * `股票代碼`
  * `進場時間` | `進場價格`
  * `出場時間` | `出場價格`
  * `進場原因` (例如：MACD 黃金交叉)
  * `出場原因` (例如：觸發 8% 停損 / 跌破月線)
  * `持有期數`
  * `單筆損益 %` | `單筆損益金額`

### 6. 交易明細圖表 (Interactive Trade Chart)
**[重要互動]** 點擊上方「交易明細清單」中的任一筆列 (Row) 時，此圖表會自動更新為該筆交易的視覺化覆盤：
* **主圖 (K線圖)：** 顯示該檔股票的 K 線走勢。明確標記「進場點 (Buy)」與「出場點 (Sell)」的圖示，並畫出連接進出點的交易線段。
* **副圖 (指標與量能)：** 顯示相關的技術指標線圖與成交量柱狀圖，並畫出對齊進出場時間點的垂直參考線。

---

## ▍設計審查問答專區（集中版）

### 🔴 衝突 1：執行頻率 vs 股票篩選策略的動態池更新成本
**問題點：** 「執行商品範圍」選擇「股票篩選策略」時，說明指出「每個時間節點只交易符合該策略的股票」。若執行頻率為 `1min` 或 `5min`，回測期間數年，每個時間步都需要重新掃描整個指標篩選策略，計算量會是 `O(T × N × I)` 完全非線性。

**建議：** 需要明確定義股票池的**更新頻率**，例如：
- 每個「執行頻率」步驟更新一次（高成本）
- 每天更新一次（固定）
- 只在回測開始時建立一次靜態池（最快最簡單）

建議在 UI 增加一個選項：「動態更新（每步）」/ 「每日更新」/ 「靜態池（回測開始時建立一次）」。

**Codex答/點評：**
1. 判斷正確，這是最關鍵的計算瓶頸。
2. 建議預設「每日更新」，並允許進階使用者切換「每步更新」。
3. 後端可加快取層：以 `(date_bucket, strategy_hash)` 作為 cache key，減少重複篩選成本。
4. 報告中應回填「股票池更新模式」，避免不同模式結果被誤比較。

**Gemini回答/點評：** 同意Codex的看法，但「每步更新」即使有快取，在長區間與短週期下仍可能導致極大的運算負載。**建議：** 預設採用「每日更新（每日收盤後重新計算次日股票池）」，並要求若週期小於 1D，應在UI跳出效能警示或限制其不能選「每步更新」。


---

### 🔴 衝突 2：最大持倉數 (Max Open Positions) 與風險管理模組的優先順序未定義
**問題點：** 若「最大同時持有檔數」設為 10，且「資金與風險管理模組」也啟用，兩個系統都可能各自決定「要不要進場」，但優先順序不明：是「先達到 10 檔就停」還是「先計算風險預算再決定是否達到 10 檔限制」？

**建議：** 明確定義執行順序：
1. 觸發進場條件
2. 飛過風險管理模組計算倉位
3. 若通過風險預算，才檢查是否超過最大持倉數
4. 全部通過才實際進場

**Codex答/點評：**
1. 同意你的順序設計，建議正式定義成單一路徑流程，避免實作時分支漂移。
2. 補充必要規則：當候選單超過持倉上限時，要有排序優先級（例如訊號強度、流動性、風險報酬比）。
3. 建議在報告新增「被拒單原因統計」（風險超標 / 持倉滿額 / 流動性不足）。

**Gemini回答/點評：** 同意單一路徑流程。對於超額持倉競爭，建議未來在介面增加「篩選優先級」設定（例如依成交量高低排列）。若不開放設定，預設可使用「依股票代碼字母順序」確保100%可重現性與無隨機性。


---

### 🟡 模糊 1：同一個 bar 的進場與出場觸發衝突
**問題點：** 若進場設定為「當期收盤價」，出場設定也為「當期收盤價」，且兩個條件在同一根 K 棒上同時觸發，會產生「同一根棒開倉又平倉，損益 ≈ 0（扣手續費）」的無效交易。

**建議：** 規定若進場條件與出場條件在同一根 K 棒被同時觸發，**優先執行進場**，出場條件延後至下一個時間步驟重新判斷。

**Codex答/點評：**
1. 同意，必須定義 deterministic 規則。
2. 建議新增引擎參數：`same_bar_policy = entry_first|exit_first|ignore_both`，避免硬編單一政策。
3. 報告需註記同 Bar 衝突筆數，便於策略除錯。

**Gemini回答/點評：** 同意Codex的 `same_bar_policy` 的做法。**補充建議：** 在日K線資料中，無法分辨高低點發生的時序。建議加上Tooltips警告使用者「日線內同時觸發條件觸發價，可能產生失真」。實作上建議預設採用 `entry_first` 保守圈存進場資金，或 `ignore_both`。


---

### 🟡 模糊 2：時間停損的顯示單位不夠直觀
**問題點：** 目前說明為「輸入 N 期（依照執行頻率計算）」。若頻率為「週 (1W)」而 N=12，使用者需要在心中換算為 12 週 = 約 3 個月。

**建議：** 輸入框旁邊即時顯示換算結果，例如：
> 最大持有時間：`[ 12 ]` 期　≈ **12 週（約 3 個月）**

**Codex答/點評：**
1. 同意，這是低成本高收益的 UX 改善。
2. 建議同時顯示「交易日估算」與「自然日估算」，避免跨市場日曆誤差。

**Gemini回答/點評：** 非常同意這是良好的 UX 改善。對於回測系統，同時顯示「交易日」與「自然日」估算，能有效降低使用者因週休二日或假期產生的認知誤判。


---

### 🟡 模糊 3：回測期間資料可用性驗證缺失
**問題點：** 若使用者設定 `start_date = 2010-01-01`，但資料庫的美股歷史資料最早只有 2019 年，系統將無聲地跑出錯誤或空白報告，沒有明確說明應如何處理。

**建議：** 在右側面板頂部（或 Start 按鈕旁）提示可用資料時間範圍。若設定的 start_date 超出資料範圍，在按下「開始回測」時顯示警告，並自動調整或阻止執行。

**Codex答/點評：**
1. 同意，建議在按下開始前做 preflight validation。
2. 驗證結果建議分級：阻止執行（硬錯誤）與可自動修正（軟警告）。
3. 需把自動修正後的實際區間寫入回測報告表頭。

**Gemini回答/點評：** 同意做事前驗證（preflight validation）。在UI互動層面，除了按下 Start，更應該在選擇日期的 Datepicker 元件上非同步獲取最大可選區間，直接將無法回測的日期 disable 掉（反灰無法點擊），從源頭防堵。


---

### 🟡 模糊 4：CAGR 的計算邊界情況
**問題點：** CAGR 定義為 `(期末值/期初值)^(1/年數) - 1`，若回測期間 < 1 年（例如只有 3 個月），算出的 CAGR 年化率可能非常誇張（如 +1200%），容易誤導使用者。

**建議：** 若回測期間 < 12 個月，將「複合年化成長率 (CAGR)」標注一個 `⚠ 回測期間不足 1 年，僅供參考` 的提示，或改為顯示「總回報率」主要指標。

**Codex答/點評：**
1. 同意，並建議把 CAGR 改為次要指標，主指標改 `Total Return` + `Max Drawdown`。
2. 若期間太短，可加顯示「日化報酬分布」取代過度放大的年化值。

**Gemini回答/點評：** 同意不強調CAGR，因為短期間CAGR放大效應會極端誤導人。反駁Codex引入「日化報酬」，由於對散戶不夠直觀，建議：若回測區間小於一年，直接將CAGR顯示為「N/A 不足一年」或直接隱藏該欄位，讓總回報率（Total Return）當唯一重點即可。


---

### 🟡 模糊 5：商品統計表的零交易股票顯示策略
**問題點：** 若某隻股票在回測期間從未觸發進場條件（0 次交易），它是否應出現在「商品統計表」中？若不顯示，使用者無法得知哪些股票「在池中但沒有成交」。

**建議：** 提供一個切換選項「只顯示有交易紀錄的股票 / 顯示全部股票池清單」，預設只顯示有成交的股票。

**Codex答/點評：**
1. 同意，建議預設只顯示有交易，並提供「包含 0 交易」切換。
2. 若顯示全池，應加一欄「未成交原因摘要」（條件未觸發 / 被風險限制 / 流動性不足），並揭露股票池覆蓋率。

**Gemini回答/點評：** 同意預設隱藏零交易標的，保持清版。建議若開啟全股票池時，可在該筆資料的 Tooltip 或明細提供「未成交原因」（如資金不足 / 最大持倉限制 / 條件未觸發），會是很好的除錯提示。


---

### 🔵 UI/UX 延伸問題（整合自《模擬交易回測_設計回饋與隱藏邏輯.md》）

### 🟠 問題 6：參數很多，是否應採漸進式揭露避免左側面板過載
**問題點：** 左側設定包含基本、進場、出場、資金與風控，首次使用者容易認知超載。

**建議：** 將非高頻設定收進「進階設定」折疊區，核心設定預設展開。

**Codex答/點評：**
1. 同意，優先級 P1。
2. 建議採「核心設定預設展開、進階設定預設收合」。
3. 需記住展開狀態，避免切頁重置造成操作中斷。

**Gemini回答/點評：** 絕對同意漸進式揭露。建議將「基本參數」、「進場設定」預設展開，而「出場設定（若未勾選停損停利前提）」與「資金與風控模組」預設折疊，可極大減少使用者一進頁面的視覺壓力。


---

### 🟠 問題 7：回測等待時間長，是否需要完整狀態回饋
**問題點：** 若只有按鈕 spinner，使用者難以判斷是正常執行、卡住或失敗。

**建議：** 增加空狀態、骨架屏、階段文字、可中止按鈕。

**Codex答/點評：**
1. 同意，優先級 P0。
2. 建議狀態機明確定義：`idle -> validating -> running -> partial -> done -> failed`。
3. `failed` 狀態要提供可操作的修正建議，而非僅顯示錯誤碼。

**Gemini回答/點評：** **反駁Codex：過度設計 (Over-engineering)。** 在以 Vanilla JS 為主的架構下，維持高達 6 種狀態 (`partial`, `validating` 等) 的非同步狀態機只會陡增開發與除錯成本。**建議 MVP 只需要 3 種狀態：** `idle -> running -> completed/failed`。畫面上只需轉圈圈並提供一個「取消」API 阻斷後端運算即可，不需要實作複雜的 partial 等級回報。


---

### 🟠 問題 8：交易明細 K 線圖是否要直接做瀏覽器全螢幕
**問題點：** 小螢幕下上下分割區域容易擁擠，覆盤細節不足。

**建議：** 增加最大化或全螢幕模式。

**Codex答/點評：**
1. 部分同意，優先級 P1。
2. MVP 先做「右側面板最大化」，不建議第一版直接做瀏覽器全螢幕 API。
3. 需補 `Esc` 退出、focus 回復來源列與 `aria-label`。

**Gemini回答/點評：** **反駁Codex：與實務脫節。** 實作自定義的「右側面板最大化」需要處理極度複雜的 Tailwind CSS `z-index`、父層 `overflow` 與浮動計算，極易弄壞現有排版。相反地，直接呼叫瀏覽器原生的全螢幕API (`element.requestFullscreen()`) 只要一行 JS 程式碼，不僅開發成本極低，圖表沉浸感也最佳，這才是最適合 MVP 的聰明做法。


---

### 🟠 問題 9：交易方向（多/空）是否應用顏色強提示
**問題點：** 方向模式若辨識不明，易造成誤判設定。

**建議：** 作多/作空分別給予綠/紅視覺暗示。

**Codex答/點評：**
1. 部分同意，優先級 P2。
2. 不可只靠顏色，需同時保留文字與 icon。
3. 建議沿用既有 token，避免新增第二套視覺語意。

**Gemini回答/點評：** 同意維持系統一致性，可沿用 `--color-up` 和 `--color-down` 配襯作多、作空字眼，避免引進第三種紅綠變體破壞專業感。輔以明顯的向上/向下 Icon 足矣。


---

### 🔵 隱藏邏輯延伸問題（整合自《模擬交易回測_設計回饋與隱藏邏輯.md》）

### 🟠 問題 10：交易費用以外，是否需加入滑點與流動性限制
**問題點：** 只計手續費但不計滑點/成交容量，回測績效會系統性偏樂觀。

**建議：** 增加滑點參數與成交量限制，並支援部分成交。

**Codex答/點評：**
1. 同意，優先級 P0。
2. 建議新增：`slippage_bps`、`max_fill_pct_of_bar_volume`、`min_dollar_volume_filter`。
3. 報告需揭露部分成交比例與被拒單原因。

**Gemini回答/點評：** 極度同意。忽略滑點會產生過度樂觀績效。一項未被提及的隱藏邏輯是「最大成交量限制」，例如條件觸發買一萬股，但當日總成交量不及一萬，系統應判定無法成交或只能「部分成交」。


---

### 🟠 問題 11：除權息/分割與價格口徑是否要明確定義
**問題點：** 若不定義 Raw/Adj 價格模式，遇到分割歷史會嚴重扭曲結果。

**建議：** 價格模式與股利處理方式需列入參數與報告表頭。

**Codex答/點評：**
1. 同意，優先級 P0。
2. 建議至少支援 `Raw`、`Split-adjusted`、`Total-return adjusted` 三種口徑。
3. 若未含股利現金流，必須在報告中明示。

**Gemini回答/點評：** 反駁Codex給三個選項的做法。讓使用者選三種口徑過於複雜且容易錯用（用Raw data算指標會產生巨大斷層）。**強烈建議** 系統統一且強制預設使用「前置復權價 (Split & Dividend Adjusted)」進行所有歷史運算與線圖繪製，這最符合常規驗證回報與技術線圖的直覺。


---

### 🟠 問題 12：資金單利/複利邏輯是否必須顯式選擇
**問題點：** 不同資金模式會顯著影響績效與風險指標，若隱藏容易誤比。

**建議：** 增加資金模式參數並顯示在報告表頭。

**Codex答/點評：**
1. 同意，優先級 P1。
2. 建議參數化：`capital_mode = fixed_notional | percent_of_equity`。
3. 與風險參數一併輸出到報告 header。

**Gemini回答/點評：** 同意應明確揭露資金模式。建議將單利/複利的觀念轉化為無痛的設定：「每次投入固定金額（恆量單利）美元」vs「每次投入可用資金（或淨值）的 N%（依比例複利）」。


---

### 🟠 問題 13：回測期末未平倉部位該如何結算
**問題點：** 期末結算口徑若不一致，勝率、回報、交易次數都可能失真。

**建議：** 定義 Force Liquidate 或 Mark-to-Market 模式。

**Codex答/點評：**
1. 部分同意，優先級 P1。
2. MVP 建議固定 `Force Liquidate`，避免 A/B 模式造成比較口徑混亂。
3. 若未來加入 B 模式，需拆已實現/未實現損益並重算對應指標。

**Gemini回答/點評：** 反駁Codex預設 Force Liquidate（強平）。對於策略而言，強平會在期末吃上一筆滑點與手續費，造成績效扭曲。建議採用 Mark-to-Market（依期末收盤市價預估未實現損益併入淨值）並加上標示，不要創造虛擬的「平倉交易手續費」。


---

### 🟠 問題 14：做空是否要同時引入借券費與保證金機制
**問題點：** 只開做空但不計 borrow fee，績效會高估；同時上 margin 則大幅提高系統複雜度。

**建議：** 先做 borrow fee，再評估 margin。

**Codex答/點評：**
1. 部分同意，優先級 P1。
2. MVP 先做借券費，帳戶型態先限 cash account。
3. margin 建議列為 P2，避免首版風控失真。

**Gemini回答/點評：** 同意先作無保證金機制的 Cash Account。做空導入借券費 (Borrow Fee) 能貼近真實，然借券費率隨需浮動，若資料庫無歷史費率，建議開出一個欄位讓使用者能自己手動設定「年化約當借券費率」參數進行扣繳測試。


---

### 🟠 問題 15：參數暫存與策略模板是否屬必做項
**問題點：** 參數繁多，若重新整理即遺失，使用體驗與效率都會下降。

**建議：** LocalStorage 草稿保存 + 策略模板管理。

**Codex答/點評：**
1. 同意，優先級 P1。
2. 草稿保存建議 debounce；資料結構需有 `schema_version`。
3. 模板操作流程應與選股頁一致（儲存/覆蓋/另存/刪除）。

**Gemini回答/點評：** **強烈反駁Codex使用 LocalStorage 的做法。** 依賴瀏覽器的 LocalStorage 暫存極不穩定，若使用者清理快取或是從電腦切換到手機查看，辛苦設定的幾十個參數會瞬間蒸發，勢必引發客訴。**唯一建議做法：** 應該比照系統已有的「股票篩選」，直接在後端開一個 `draft_strategies` 資料表，只要參數變動就非同步更新到資料庫，實現跨裝置無縫接軌。


---

### 🟣 額外新增問題與觀點（原兩份文件未完整覆蓋，問答版）

### 🟣 問題 16：生存者偏差（Survivorship Bias）是否會扭曲結果
**問題點：** 若股票池只含目前仍上市標的，退市股與失敗企業會被排除，績效偏樂觀。

**建議：** 使用含退市標的的歷史成分資料，並在報告揭露資料口徑。

**Codex答/點評：**
1. 同意，屬核心可信度風險。
2. 建議在報告加入「是否含退市標的」欄位。

**Gemini回答/點評：** 非常同意這是量化系統的痛點。但若當前免費取得的資料庫本就缺乏已下市美股資料，強求無用。MVP 的解法建議在面板加入免責聲明「⚠ 本系統不含退市公司歷史資料，長週期回測可能有生存者偏差高估績效」，幫使用者打預防針即可。


---

### 🟣 問題 17：Look-ahead bias 與指標暖機期是否已被正確處理
**問題點：** 若指標計算偷看到未來資料，或暖機不足就交易，回測會失真。

**建議：** 明確定義暖機期間、資料截斷規則與可交易起始點。

**Codex答/點評：**
1. 同意，需在引擎層硬性保證。
2. 建議回報每檔標的有效起算時間，避免誤讀策略覆蓋範圍。

**Gemini回答/點評：** 完全同意。技術實作上：假如使用者設定回測開始為 2023/01/01 且設定 MA200，後端引擎必須聰明地往前撈約一年的歷史資料算指標「暖機」。前端 UI 應說明「首筆實際觸發交易將視指標計算的資料起算點而定」。


---

### 🟣 問題 18：時區、交易日曆、半日市處理是否一致
**問題點：** 美股 DST 切換與半日市若處理不一致，會造成進出場時間錯位。

**建議：** 引擎統一採交易所時區，並以交易日曆驅動可交易時間窗。

**Codex答/點評：**
1. 同意，需定義為平台級規則。
2. 報告建議輸出「時區 / 日曆版本」以利追溯。

**Gemini回答/點評：** 同意這項底層基礎建設。建議所有運算的 Datetime 均強迫轉換並基於交易所時區 (美股=US/Eastern)。前端頁面展示日期給使用者時，可依需求對轉換為本地時間 (Local Time)，但內部統一標準化時區以杜絕爭議。


---

### 🟣 問題 19：資料缺口、停牌、異常值是否有統一政策
**問題點：** 缺 Bar、極端跳價、停牌日若處理不一致，策略結果難比較。

**建議：** 建立缺值補齊/剔除政策與異常值閾值，統一於引擎層處理。

**Codex答/點評：**
1. 同意。
2. 建議在報告增加資料品質摘要（缺值率、剔除筆數、停牌天數）。

**Gemini回答/點評：** 同意。最常見做法是採用 Forward Fill：前一交易日的收盤價格延續到停牌日，並將當日成交量設為零。這屬資料正規化的底層邏輯，不可交由前端判斷。需在後端確立這個 Pipeline。


---

### 🟣 問題 20：成交模型（市價/限價/部分成交）是否可重現
**問題點：** 若未定義成交優先與取消規則，同參數可能每次回測結果不同。

**建議：** 明確委託狀態機（submitted/partial/filled/cancelled）與撮合規則。

**Codex答/點評：**
1. 同意，這是回測引擎可信度核心。
2. 建議加入 deterministic tie-break 規則與固定隨機種子（若有隨機過程）。

**Gemini回答/點評：** 絕對同意決定論（Deterministic）機制。強烈反對引入亂數隨機種子，因為回測必須 100% 確保「同人同參數每一次跑的結果完全相同」。所以若發生物件排序平手時，唯一的解決方案是用穩定的鍵值（如股票代碼字母順序）決定。


---

### 🟣 問題 21：多標的同時觸發時，資金競爭規則是否固定
**問題點：** 若同時有多個候選標的，排序不同會造成結果差異。

**建議：** 定義候選排序鍵（訊號強度、流動性、風險報酬）與平手規則。

**Codex答/點評：**
1. 同意。
2. 建議報告輸出「候選/錄取/拒單」統計與主要拒單原因。

**Gemini回答/點評：** 若無法開放使用者自定義權重，預設以「近期日均成交量大小」來排序是最佳的策略。因為當面臨資金競爭時，成交量大的標的最不可能因為流動性與滑點被淘汰，這也最貼近真實市場中大資金的配置現實。


---

### 🟣 問題 22：可重現性（Reproducibility）是否可追溯到版本層級
**問題點：** 缺少資料版本與引擎版本記錄時，歷史報告難以重跑驗證。

**建議：** 報告附 `engine_version`、`data_snapshot_id`、`param_hash`。

**Codex答/點評：**
1. 同意。
2. 建議將上述欄位列為報告必填 metadata。

**Gemini回答/點評：** 同意保留 Metadata。長遠應用而言，甚至可支援回測結果匯出 `.json` 功能，讓使用者可以一鍵「載入別人分享的高效策略」，以利社群或內部交流的方便性。


---

### 🟣 問題 23：過度擬合治理是否納入流程
**問題點：** 若只看樣本內績效，容易挑到偶然有效的參數。

**建議：** 至少支持樣本內/外切分，進階可加 walk-forward。

**Codex答/點評：**
1. 同意。
2. 建議報告同時展示 in-sample 與 out-of-sample 指標對照。

**Gemini回答/點評：** 反駁Codex在現階段實作 Out-of-Sample（OOS）。在 MVP 提供一個太複雜的 OOS 設定或 Walk-forward，雖然專業但會徹底毀滅非計量使用者的學習曲線。建議先讓使用者手動切換時間段進行分段實驗就好。


---

### 🟣 問題 24：退市股票與公司行動事件的清算日處理
**問題點：** 退市、合併、私有化等事件若不定義清算規則，持倉可能卡死或估值錯誤。

**建議：** 建立 corporate-event 清算策略（強平、換股、現金結算）並明確紀錄。

**Codex答/點評：**
1. 同意，這是常被忽略但高影響風險。
2. 建議事件處理結果寫入交易明細原因欄位。

**Gemini回答/點評：** 如果資料庫未來收錄了下市資料，這條規則極度關鍵。遭遇退市事件的前一個實體交易日該股之收盤價，必須被系統強制用於「退市平倉結算」，清空部位，否則總資金可能被永遠被鎖死在下市的死部位中無法輪轉。

---

### 🟣 問題 25：參數版本相容與前後端 schema 演進
**問題點：** 參數欄位未版本化時，舊草稿或舊模板可能無法正確載入。

**建議：** 請求與模板 payload 皆加 `schema_version`，提供 migration 邏輯。

**Codex答/點評：**
1. 同意。
2. 建議 migration 失敗時提供可修復提示，不直接丟棄使用者設定。

**Gemini回答/點評：** Schema Version 的設計是基礎最佳實踐。這確保未來系統新增進出場機制後，讀取到舊有的 Template 不會引發崩潰，只要實作優雅降級（未知參數帶入預設值），並發跳窗告知「舊版資料部分設定已依照新版重製」即可。

---

---

### 🟣 問題 26：資料支援度與隱藏週期
**問題點：** 目前系統的資料庫是否已齊備「分時層級（1min, 5min 等）」的美股資料？若無，前端介面提供這些選項將導致執行錯誤。

**Codex答/點評：**
1. 同意，這是「UI 可選能力」必須嚴格對齊「資料可用能力」的典型問題。
2. 建議不要只用前端隱藏選項，而是由後端回傳 capability（可用頻率清單），前端僅渲染可用項，避免版本不同步。
3. 若使用者載入舊策略且包含目前不可用頻率，應自動降級到最近可用頻率並顯示醒目提示。
4. 回測報告需記錄實際執行頻率與資料來源層級（daily/intraday），避免結果解讀錯位。

**Gemini回答/點評：** 若目前僅有「日級別 (Daily)」，則UI的「執行頻率」應暫時隱藏短週期選項，避免前端可以選，但後端撈不到資料而報錯。若未來擴充資料，再開放此欄位。

---

## ▍版面排版建議（維持現有風格）

風格基準：參考「股票篩選」分頁的深色主題（左側控制面板 + 右側內容區）。

### 整體結構
```
┌────────────────────────────┬──────────────────────────────────────────────────────┐
│   左側：回測參數設定面板       │  右側：回測報告面板（Tabs 切換）                           │
│   (固定寬度，約 280–320px)   │                                                      │
│                            │  ┌──────────────────────────────────────────────────┐ │
│ ── 基本回測參數 ──           │  │ Tab: 概覽  |  商品分析  |  交易明細               │ │
│   回測期間:                  │  └──────────────────────────────────────────────────┘ │
│   [1D][1W][1M][3M][6M][1Y]  │                                                      │
│   [自訂] → [日期]~[日期]     │  【概覽 Tab】                                          │
│   執行頻率: [下拉選單]        │  ┌─ 回測資訊表頭（4欄卡片）──────────────────────────┐  │
│   交易方向: ○ 做多 ○ 做空   │  │  期間 | 執行時間 | 商品範圍 | 交易設定摘要           │  │
│   執行商品: [選項卡]         │  └──────────────────────────────────────────────────┘  │
│ ── 進場設定 ──              │  └──────────────────────────────────────────────────┘  │
│   進場觸發：[摘要卡] [+]    │  ┌─ 整體統計（數字卡片 Grid 排列）───────────────────┐  │
│     MA-日K:MA20>MA60 ✏🗑  │  │  勝率 | 盈虧比 | 總回報率 | CAGR                 │  │
│   進場限制（組合式）：       │  │  總交易 | 獲利 | 虧損次數 | 最大回撤              │  │
│     跳空[▼]達[ ]%即[▼]    │  └──────────────────────────────────────────────────┘  │
│     開盤後[ ][▼][▼]        │  ┌─ 淨值曲線圖（互動折線圖）──────────────────────────┐  │
│   進場價格：                │  │                                                  │  │
│   ○ 次期開盤 ○ 當期收盤    │  │                 (LightweightCharts 折線 + 0% 線) │  │
│   ○ 條件觸發價              │  └──────────────────────────────────────────────────┘  │
│   最大持倉：[數字輸入]       │                                                      │
│                            │  【商品分析 Tab】                                       │
│ ── 出場設定 ──              │  ┌─ 商品統計表（可排序資料表格）────────────────────┐   │
│   條件出場：[摘要卡] [+]    │  │  代碼 | 交易次數 | 勝率 | 貢獻損益金額 | 貢獻損益% │  │
│   停利：☐ [ ]%             │  │  (每行可點擊 → 自動切換至交易明細 Tab)            │  │
│   停損：☐ [ ]%             │  └──────────────────────────────────────────────────┘  │
│   時間停損：☐ [ ]期 ≈N週   │                                                      │
│   出場價格：                │  【交易明細 Tab】                                       │
│   ○ 次期開盤 ○ 當期收盤    │  ┌─ 交易明細清單（上方可篩選排序）──────────────────┐   │
│   ○ 條件觸發價              │  │  代碼 | 進場 | 進場價 | 出場 | 出場價 | 原因 | 損益│   │
│                            │  │  (每行可點擊 → 自動切換至交易明細 Tab)            │  │
│ ── 資金設定 ──              │  └──────────────────────────────────────────────────┘  │
│   初始資金：[$1,000,000]    │                                                      │
│   ☐ 啟用風險管理模組        │  【交易明細 Tab】                                       │
│   交易費用：[ ]% / $固定    │  ┌─ 交易明細清單（上方可篩選排序）──────────────────┐   │
│                            │  │  代碼 | 進場 | 進場價 | 出場 | 出場價 | 原因 | 損益│   │
│ ┌──────────────┐           │  └──────────────────────────────────────────────────┘  │
│ │  ▶ 開始回測  │           │  ┌─ 交易 K 線圖（點擊明細後更新）────────────────────┐  │
│ └──────────────┘           │  │  主圖：K線 + 進出場標記 + 連線                   │  │
│                            │  │  副圖：指標線圖 + 成交量 + 進出場垂直線           │  │
│                            │  └──────────────────────────────────────────────────┘  │
└────────────────────────────┴──────────────────────────────────────────────────────┘
```

### ⏳ 回測執行中與空狀態 (Progress & Empty State) 生動 UI 設計
為了徹底解決**問題 7** 提到的「等待時間長、卡住感」問題，針對純前端的 MVP 架構（缺少複雜 SSE 推播的狀況下），我們需要在介面上加入以下 UX 設計，確保使用者時刻擁有系統正在運作的安心感：

1. **初始空狀態 (Empty State)：**
   - 右側主畫面一開始不要顯示 `NaN` 或空白圖表。
   - 在正中央居中放置一個脈衝 (Pulse) 動畫大圖示（例如使用主色 `--color-primary` 發光），下方搭配提示文字：「👈 請由左側設定策略參數，然後點擊 開始回測」。

2. **運算中狀態 (Loading / Progress Bar)：**
   - 當按下 `[ ▶ 開始回測 ]` 且 API Request 出去後：
     - 左下角按鈕必須**強制轉變為** `[ ⏹ 取消執行 ]` (邊框顯示閃爍的危險色 `--color-down`)，並阻斷連續點擊。
     - 右側顯示區彈出一個精緻的半透明毛玻璃背景 (Backdrop blur)，中央會浮現出一個**具有真實量化數據的動態進度條 (Progress Bar)**。
     - **真實進度條設計 (需後端 SSE 支援)**：透過後端串流推播，進度條內部會即時顯示填滿的百分比區塊與數值（例如：`68%`）。
     - **預估時間計算邏輯 (ETA)**：利用前端接收到的每一次 `(進度增量, 耗時增量)` 計算平均速度，並即時運算推導出「預估剩餘時間（約 12 秒）」，顯示於條塊上方，給予最精確的倒數期盼感。
     - **文字安定感設計 (Micro-copy)**：在閃爍進度軌的下方，每 2 秒自動輪播洗腦文字（例如：「正在向資料庫撈取歷史 K 線...」➜「正在逐日掃描進出場條件...」➜「模擬撮合與結算中...」）。就算有真實進度條，這項**純前端魔法**依然可以維持畫面的豐富動態感，能讓使用者的等待體感時間銳減 50%！非常建議保留此設計。

#### 佈局示意圖 (Empty State & Progress/Loading State)

**【初始空狀態 (Empty State)】**
```text
┌────────────────────────────┬──────────────────────────────────────────────────────┐
│   左側：回測參數設定面板       │  右側：未開始回測前的空白主畫面 (Empty State)             │
│   (固定寬度，約 280–320px)   │                                                      │
│                            │                                                      │
│ ── 基本回測參數 ──           │                                                      │
│   回測期間:                  │                                                      │
│   [1D][1W][1M][3M][6M][1Y]  │                  (✨ 會呼吸發光的 Icon)                    │
│   [自訂] → [日期]~[日期]     │                                                      │
│                            │                                                      │
│        (略...)             │                                                      │
│                            │             「 👈 請由左側設定策略參數，                │
│                            │                    然後點擊開始回測 」                   │
│ ── 資金設定 ──              │                                                      │
│   初始資金：[$1,000,000]    │                                                      │
│                            │                                                      │
│ ┌──────────────┐           │                                                      │
│ │  ▶ 開始回測  │           │                                                      │
│ └──────────────┘           │                                                      │
└────────────────────────────┴──────────────────────────────────────────────────────┘
```

**【運算中狀態 (Running State)】**
```text
┌────────────────────────────┬──────────────────────────────────────────────────────┐
│   左側：回測參數設定面板       │  右側：進入毛玻璃遮罩狀態 (Backdrop Blur)                 │
│   (參數狀態 Disabled 鎖定)   │                                                      │
│                            │                                                      │
│ ── 基本回測參數 ──           │                                                      │
│   🔒 回測期間:               │                                                      │
│   [1D][1W][1M][3M][6M][1Y]  │                  ⏳ 預估剩餘時間：約 12 秒                 │
│   [自訂] → [日期]~[日期]     │             ⚡ 回測引擎正在暴風運算中...                   │
│                            │                                                      │
│        (略...)             │          ┌────────────────────────────────┐          │
│                            │          │█████████████████        [ 68% ]│          │
│ ── 資金設定 ──              │          └────────────────────────────────┘          │
│   🔒 初始資金：[$1,000,000] │                                                      │
│                            │                                                      │
│ ┌──────────────┐           │            「 正在日線級別逐根掃描進出場條件... 」          │
│ │  ⏹ 取消執行  │ (亮危險色)│           (隨後端狀態切換提示字眼)                       │
│ └──────────────┘           │                                                      │
└────────────────────────────┴──────────────────────────────────────────────────────┘
```

### 版面設計要點
1. **左側面板**：與篩選頁面左側一致，同樣使用 `position: sticky; top: 0`，高度 `100vh - topbar`，overflow-y: auto 捲動；各設定區塊使用折疊 `<details>` 或分隔線區塊。
2. **回測資訊表頭**：4 個數字卡片橫向排列（同現有篩選結果上方的 `統計結果` row 風格）。
3. **整體統計**：使用 8～12 個小卡片 (`grid: repeat(auto-fill, minmax(120px, 1fr))`)，正值綠色 / 負值紅色，沿用現有色彩變數 `--color-up` / `--color-down`。
4. **淨值曲線圖**：採用 LightweightCharts `addAreaSeries`（與 K 線圖保持統一技術棧），高度約 200–280px，固定顯示 0% 水平基準線。
5. **商品統計表**：與現有篩選結果的股票列表一致，支援欄位點擊排序；行點擊後自動切換至「交易明細 Tab」並預篩選該股票的明細。
6. **交易明細 Tab 上下分割**：上方明細列表（~40% 高度）+ 下方 K 線圖（~60%），中間加入**垂直拖曳調整高度**的 resize handle（與現有篩選頁面一致）。
7. **「開始回測」按鈕**：固定在左側面板底部，使用現有系統的 `--accent` 顏色。必須設定 `position: sticky; bottom: 0; z-index: 10;` 使其永遠貼底懸浮確保隨時可見，避免使用者需要往下捲動才能按；執行中應配合顯示進度條（SSE streaming），且支援中途停止。
8. **左側面板收合優化 (Hamburger Menu Collapse)**：與篩選頁面一致，支援點擊左上角漢堡選單收起左側面板。**關鍵 UX**：當左側收起時，右側畫面最上方應出現一列極簡的「目前參數摘要 Badge」（例如：`[MA交叉策略] [1D] [資金: $1M]`），確保使用者在看全螢幕報表時不會忘記測試條件。
9. **右側純淨的 Tab 切換機制**：回測報表厚重，千萬不要把所有圖表跟明細堆疊上下捲動。應採用明顯的 Tab 切換：`[ 📊 綜合績效概覽 ]`、`[ 📋 交易明細與分析 ]`、`[ 📈 逐筆 K 線覆盤 ]`，一次只專注一種資訊。