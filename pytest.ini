[pytest]
pythonpath = .
testpaths = tests
addopts = -v --tb=short
markers =
    unit: 純邏輯單元測試，無需外部依賴
    guard: 檔案存在性/內容守護測試
    integration: 需要 FastAPI 伺服器運行
    smoke: 需要 DB 且受環境變數控制
    slow: 執行時間較長的測試
