# K 線圖設定功能 — New Features 規格書
> 建立日期：2026-03-17　　最後更新：2026-03-17（加入討論紀錄與決策說明）

---

## 附件說明
附有多張截圖作為 UI 排版與預期行為的視覺參考（p5、p6-1、p6-2、p7、p8）。

---

## ✨ New Features

---

### Feature 1：新增「指標介紹」內文

**影響檔案：** `chartSettingsModal.js`

**需求：**
在指標管理介面的「MA」和「BOLL」設定頁中，新增可切換的「指標介紹」子頁籤。

**實作邏輯：**
- 移除「指標介紹」tab button 的 `disabled` 屬性
- 加入 tab 切換事件綁定（`_activeSettingsTab` 追蹤當前頁籤）
- 新增 `renderMAIntro()` 方法，包含完整 MA 說明文字（葛氏八法則等，如圖 p5）
- 新增 `renderBOLLIntro()` 方法，包含計算公式與應用法則（如圖 p6-1、p6-2）

---

### Feature 2：圖表管理新增「型態管理」Tab

**影響檔案：** `templates/ChartSettingsModalTemplate.js`、`chartSettingsModal.js`、`function_block/pattern_annotation.js`

**需求：**
在「圖表管理」原有的 Tab 列右側新增「型態管理」Tab。

**管理項目（共 5 種型態）：**
盤整區、W底、頭肩頂、頭肩底、三角收斂

**每種型態的控制維度：**

| 控制項 | 說明 |
|--------|------|
| 標註型態勾選框 | 決定是否顯示型態框線或趨勢線 |
| 標註型態顏色 | 框線或趨勢線的顏色 |
| 不透明度 | 0–100 滑桿 |
| 標註型態文字勾選框 | 決定是否顯示型態名稱標籤 |
| 標註型態文字顏色 | 名稱標籤的顏色 |

**實作邏輯：**
- 在 template 中新增 Tab button `data-tab="patterns"`
- 新增 `defaultPatternVisualConfig`（5 個型態的預設值）
- `renderPatternSettings()` 渲染每個型態的設定列
- `PatternAnnotation.setConfig(cfg)` 方法：在繪製時套用顏色、不透明度、可視性
- `apply()` 將 patternConfig 存入 `localStorage`，並呼叫 `PatternAnnotation.setConfig()`

**Tab 佈局切換邏輯：**
- 切換到「型態管理」Tab 時，左側 sidebar 顯示 5 種型態名稱（動態替換，非靜態 HTML）
- 切換到「指標管理」Tab 時，sidebar 恢復顯示指標列表

---

### Feature 3：圖表管理補充「常規設定」Tab

**影響檔案：** `templates/ChartSettingsModalTemplate.js`、`chartSettingsModal.js`、`chartController.js`

**需求：**
新增常規設定面板，**排版嚴格參考附圖 p7**。

**控制項規格：**

| 控制項 | 類型 | 說明 |
|--------|------|------|
| 現價線 | 勾選框 | 控制是否顯示最新價格的虛線 |
| 十字線 | 下拉選單 | 選項：關閉、懸浮窗 |
| 主圖類型 | 下拉選單 | 選項：普通K線、美國線、收盤價線、四式陰陽燭、平均K線 |
| 陽線設定 | 下拉選單 | 選項：空心陽線、實心陽線 |

**已確認的實作細節：**

**主圖類型（已決策）**：所有類型皆使用現有 `CandlestickSeries`，**不替換 Series 類型**，透過 `series.applyOptions()` 改變顏色樣式：

| 主圖類型 | 實作方式 |
|---------|----------|
| 普通K線 | 預設 upColor/downColor（有顏色 K 棒） |
| 美國線 | `wickVisible: true`，upColor/downColor 調為線框顯示 |
| 收盤價線 | 新增一條 `LineSeries` 疊加顯示（不換掉 candleSeries） |
| 四式陰陽燭 | 調整陽/陰線的 upColor、borderUpColor、wickUpColor 切換 |
| 平均K線 | 預計算 Heikin-Ashi OHLC 後重設 candleSeries 資料 |

> ⚠️ **重要**：PatternAnnotation 使用 `candleSeries.priceToCoordinate()` 計算座標，切換類型時若涉及資料重設，需同步觸發 `PatternAnnotation.render()`。

**十字線「懸浮窗」功能（已決策）：**
- 第一版只顯示：開、高、低、收、漲跌額（= close - open）、成交量
- 換手率、市盈率：暫顯示 `N/A`，加 `// TODO: 需額外 API` 備考

**儲存方式：** `localStorage`（與 MA 設定一致）

---

### Feature 4：圖表管理補充「坐標軸」Tab

**影響檔案：** `templates/ChartSettingsModalTemplate.js`、`chartSettingsModal.js`、`chartController.js`

**需求：**
新增坐標軸控制面板，**排版嚴格參考附圖 p8**。

**控制項規格：**

| 控制項 | 類型 | 選項 |
|--------|------|------|
| 坐標切換 | 下拉選單 | 普通坐標、對數坐標、百分比坐標、等比坐標 |
| 坐標設定 | 下拉選單 | 左坐標、右坐標、雙邊坐標 |
| 左坐標資料來源 | Radio Button | 價格 / 漲跌幅 |
| 右坐標資料來源 | Radio Button | 價格 / 漲跌幅 |

**坐標切換對應 LightweightCharts API（已確認）：**

| UI 選項 | LightweightCharts 模式 | 說明 |
|---------|----------------------|------|
| 普通坐標 | `PriceScaleMode.Normal` (0) | 線性刻度，等距絕對金額 |
| 對數坐標 | `PriceScaleMode.Logarithmic` (1) | 對數刻度 |
| 百分比坐標 | `PriceScaleMode.Percentage` (2) | 顯示相對可見起點的 % |
| 等比坐標 | `PriceScaleMode.Logarithmic` (1) | **與富途牛牛等比坐標完全一致** |

**富途牛牛等比坐標確認說明：**
- 富途牛牛的「等比坐標」本質為對數刻度，Y 軸各格代表等比例漲跌幅，而非固定絕對金額
- LightweightCharts 的 `PriceScaleMode.Logarithmic` 與此行為完全吻合，無需自訂 priceFormatter
- 對數坐標與等比坐標在本實作中映射到同一個 mode（視覺上相同），若日後需區分，可透過自訂 priceFormatter 標籤差異化

**儲存方式：** `localStorage`（與 MA 設定一致）

---

### Tab 整體佈局切換規格

| Tab 名稱 | 左側 Sidebar | 右側內容 |
|---------|------------|----------|
| 指標管理 | 顯示（指標列表） | 指標設定面板 |
| 型態管理 | 顯示（型態列表，動態替換） | 型態設定面板 |
| 常規設定 | **隱藏** | 單欄全寬面板 |
| 坐標軸 | **隱藏** | 單欄全寬面板 |

---

## 決策紀錄

| 議題 | 決策 |
|------|------|
| 主圖類型「四式陰陽燭」 | 不替換 Series，改用 `applyOptions()` 調整顏色樣式模擬 |
| 主圖類型切換後 PatternAnnotation | 切換後需同步觸發 `render()`，確保座標計算正確 |
| 懸浮窗第一版範圍 | 開高低收 + 漲跌額 + 成交量；換手率/市盈率顯示 N/A + TODO |
| 等比坐標實作 | 直接用 `PriceScaleMode.Logarithmic`，與富途行為完全一致 |
| 新功能儲存位置 | `localStorage`，與現有 MA 設定相同機制 |

